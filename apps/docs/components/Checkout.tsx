"use client";
import React, { useEffect, useMemo, useRef, useState } from "react";

export default function Checkout () {
    return (
        <div style={{height: 'calc(100vh - 100px)'}}>Hello Checkout</div>
    )
}